{
  "name": "auvide",
  "private": true,
  "version": "0.2.0",
  "description": "AI video upscaler + vibrant HDR10 remapper",
  "type": "module",
  "scripts": {
    "stage-engine": "node scripts/stage-engine.mjs",
    "stage-uv-sidecar": "node scripts/stage-uv-sidecar.mjs",
    "generate-contracts": "node scripts/generate-contracts.mjs",
    "dev": "vite",
    "build": "bun run generate-contracts --check && tsc && vite build",
    "test": "bun test",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
