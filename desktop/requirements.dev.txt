# Dev tooling for the auvide engine (pytest, ruff, mypy) plus the legacy GUI's
# runtime deps. The dev extra is declared once in ../engine/pyproject.toml —
# this file just pulls it in so `pip install -r requirements.dev.txt` still
# works from this directory.
#
# Install:  pip install -r requirements.dev.txt

-e ../engine[dev]
pillow>=10.0
